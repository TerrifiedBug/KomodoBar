import Foundation

/// Shared, UI-free constants for KomodoBar, used by both the app and the CLI.
public enum KomodoBarCore {
    /// Fallback marketing version for dev builds (the real one comes from Info.plist).
    public static let fallbackVersion = "0.1.0"
}
